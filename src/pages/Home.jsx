import React from 'react'

export default function Home(){
  return (
    <div className="prose">
      <h1>Purchase Orders</h1>
      <p>Use the header to switch between Manager, Buyer, and Admin views. This scaffold uses an in-browser fake API (MirageJS). Mobile-first, simple, and ready to connect to a real backend by changing <code>src/config.js</code>.</p>
    </div>
  )
}
